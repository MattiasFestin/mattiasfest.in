{
  "name": "mattiasfest.in",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/optimize-png.mjs && zola build && node scripts/run-python.mjs && node scripts/render-math.mjs && node scripts/optimize.mjs && node scripts/fingerprint.mjs",
    "render:manim": "sh manim/render.sh 0001 --final && sh manim/render.sh 0005 --final",
    "render:manim:cached": "sh manim/render.sh 0001 --final --if-missing && sh manim/render.sh 0005 --final --if-missing",
    "build:ci:site": "node scripts/prune-scheduled.mjs && npm run build",
    "build:ci": "npm run render:manim && npm run build:ci:site",
    "build:ci:cached": "npm run render:manim:cached && npm run build:ci:site",
    "dev": "node scripts/dev.mjs",
    "test": "playwright test",
    "test:ci": "playwright test --grep-invert @external",
    "test:external": "playwright test --grep @external"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "clippyjs": "^0.1.0",
    "esbuild": "^0.28.1",
    "pngjs": "^7.0.0",
    "temml": "^0.13.3",
    "webamp": "^2.3.1"
  }
}
