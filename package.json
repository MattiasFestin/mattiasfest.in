{
  "name": "mattiasfest.in",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/optimize-png.mjs && zola build && node scripts/run-python.mjs && node scripts/render-math.mjs && node scripts/optimize.mjs && node scripts/fingerprint.mjs",
    "render:manim": "node scripts/render-published-manim.mjs",
    "render:manim:cached": "node scripts/render-published-manim.mjs --if-missing",
    "prune:scheduled": "node scripts/prune-scheduled.mjs",
    "build:ci:site": "npm run prune:scheduled && npm run build",
    "build:ci": "npm run prune:scheduled && npm run render:manim && npm run build",
    "build:ci:cached": "npm run prune:scheduled && npm run render:manim:cached && npm run build",
    "dev": "node scripts/dev.mjs",
    "test": "playwright test",
    "test:ci": "playwright test --grep-invert @external",
    "test:external": "playwright test --grep @external"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "clippyjs": "^0.1.0",
    "esbuild": "^0.28.1",
    "pngjs": "^7.0.0",
    "temml": "^0.13.3",
    "webamp": "^2.3.1"
  }
}
