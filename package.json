{
  "name": "mattiasfest.in",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "zola build && node scripts/render-math.mjs && node scripts/optimize.mjs",
    "dev": "node scripts/dev.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "pngjs": "^7.0.0",
    "temml": "^0.13.3"
  }
}
